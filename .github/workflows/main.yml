name: Mobile Test Automation - Calendar App

on:
  workflow_dispatch:

jobs:
  run-tests:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Run tests
        run: mvn test